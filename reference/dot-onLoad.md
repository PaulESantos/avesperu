# .onLoad hook

Hook function that runs when the package is loaded. It sets default
options for the package.

## Usage

``` r
.onLoad(libname, pkgname)
```

## Arguments

- libname:

  A character string with the name of the library directory.

- pkgname:

  A character string with the name of the package.
