% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny-app.R
\name{run_avesperu_app}
\alias{run_avesperu_app}
\title{Run a TNRS-style Shiny app for avesperu}
\usage{
run_avesperu_app(
  host = "127.0.0.1",
  port = NULL,
  launch.browser = interactive()
)
}
\arguments{
\item{host}{Host interface passed to \code{\link[shiny:runApp]{shiny::runApp}}.
Default: \code{"127.0.0.1"}.}

\item{port}{Port passed to \code{\link[shiny:runApp]{shiny::runApp}}.
Default: \code{NULL} (Shiny selects a free port).}

\item{launch.browser}{Logical; passed to
\code{\link[shiny:runApp]{shiny::runApp}}. Default: \code{interactive()}.}
}
\value{
The value returned by \code{\link[shiny:runApp]{shiny::runApp}}.
}
\description{
Launches an interactive Shiny application for batch resolution of bird
scientific names against the \code{avesperu} checklist. The interface is inspired
by the BIEN TNRS workflow, but uses the local \code{avesperu} dataset and matching
engine.
}
\details{
The app supports:
\itemize{
\item Batch input via pasted text or uploaded CSV/TXT/TSV files
\item Name parsing and standardization
\item Exact or fuzzy matching through \code{\link{search_avesperu}}
\item Interactive review of matches and export of results and metadata
}

Synonym retrieval is not currently available because \code{avesperu} ships the
accepted Peru checklist, not a synonymy backbone.
}
\examples{
if (interactive()) {
  run_avesperu_app()
}
}
